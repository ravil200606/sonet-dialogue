spring:
  datasource:
    url: 'jdbc:h2:mem:testdb'
    username: 'sa'
    password: 'password'
    driver-class-name: 'org.h2.Driver'
  h2:
    console:
      enabled: true
  jackson:
    date-format: 'yyyy-MM-dd'
server:
  port: 8081